apiVersion: v1
kind: Service
metadata:
  name: {{ include "mollys-movies.ui.fullname" . }}
  labels:
    {{- include "mollys-movies.labels" . | nindent 4 }}
spec:
  type: {{ .Values.ui.service.type }}
  ports:
    - port: {{ .Values.ui.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "mollys-movies.ui.selectorLabels" . | nindent 4 }}
